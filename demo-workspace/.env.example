# Generado automáticamente por install/deploy-demo.sh al final del despliegue.
# Copia este archivo a .env solo si necesitas setear los valores a mano
# (por ejemplo, para apuntar a un RHDH ya desplegado sin volver a correr el script).
RHDH_MCP_URL=https://<rhdh-host>/api/mcp-actions/v1
RHDH_MCP_TOKEN=<token>
